{% extends 'base.html.twig' %}

{% block title %}Hello FlatController!{% endblock %}

{% block body %}


    <table class="table">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Liczba wolnych miejsc</th>


            <th scope="col">Actions</th>
        </tr>
        </thead>
        <tbody>

        {% for flat in flats %}
            <tr>
                <th scope="row">{{ flat.id }}</th>
                <td>{{ flat.slots }}</td>
                <td><a href="{{ path('app_order_show',{'id':flat.id}) }}" class="btn btn-outline-primary">zamów miejsce</a></td>
            </tr>
        {% endfor %}

        </tbody>
    </table>

{% endblock %}
